{
  "name": "hello-github-actions",
  "version": "1.0.0",
  "scripts": {
    "test": "node test.js",
    "build": "echo 'Building project...' && mkdir dist && cp index.js dist/"
  },
  "dependencies": {},
  "devDependencies": {}
}
